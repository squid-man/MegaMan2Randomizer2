name: Release Dev Build
on:
  push:
    branches:
      - master
      - main
      - develop
      - ava-update4

jobs:
  build:
    uses: ./.github/worlflows/build-core.yml
    with:
      target-folder: dev
      is-official: false